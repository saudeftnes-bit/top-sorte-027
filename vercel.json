{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": null,
    "rewrites": [
        {
            "source": "/api/efi-webhook/pix",
            "destination": "/api/efi-webhook"
        },
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        }
    ]
}